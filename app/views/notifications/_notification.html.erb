<li>
  <% unless notification.is_read? %>
    <strong>New</strong>
  <% end %>
  <small><%=time_ago_in_words(notification.created_at) %> ago</small>
  <a href="<%= notification_url(notification) %>"><%= notification.text %></a>
  <button>ADD --delete</button>
</li>