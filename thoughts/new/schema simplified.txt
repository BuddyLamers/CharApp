table: users(
username
email
password_digest
session_token
)

table: characters(
name
tagline  --short description showable on main page.
private?
user_id
forked_id
)

table: character_details(
character_id
title
body
)

table: comments(
author_id
character_id
body
)

table: forks(
from_character_id
to_character_id
)

table: stars/favorites(
character_id
user_id
)

--ask about this...if it is databased right.
table: messages(
sender_id
receiver_id
title
body
)

table: character_subscriptions(
subscriber_id
character_id
)

table: user_subscriptions(
user_id
subscriber_id
)

