forking a character DOES fork details, but fails a validation
  as it does not give the details to Character.create
    --solved by removing Character presence val for detail

deleting a forked character DELETES THE ORIGINAL
  --solved by changing its id

forking a character forks its comments
  --solved by last thing, removed Char pres val for comment


  instantiate duplicate details
  --checked what detail_params returns, and create similar format


cannot fork a character if you have already forked it
  or you own it
    --finished

comment body not displaying on chracter show page
  --fixed by fixing other problems